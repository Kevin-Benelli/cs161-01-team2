{
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd api && nodemon socket-server.js && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run client\"",
    "heroku-postbuild": "cd client && npm install && npm run build",
    
  }
}
